[build]
  functions = "netlify/functions"  # ← CHANGED from "users"
  command = ""                     # ← ADD THIS - disables expo build
  publish = "netlify"              # ← ADD THIS - deploys functions only

[[redirects]]
  from = "/api/*"
  to = "/.netlify/functions/:splat"
  status = 200
